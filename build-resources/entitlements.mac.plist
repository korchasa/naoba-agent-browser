<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  What the application needs from the hardened runtime.

  Chromium compiles JavaScript as it runs and maps the result executable, which
  the hardened runtime forbids by default; the library rule is what lets the
  Electron helpers load frameworks signed for this application rather than for
  the system. Nothing here weakens the sandbox, because the application does not
  run in one: it is a browser, and the pages it opens are the untrusted part.
-->
<plist version="1.0">
<dict>
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.allow-dyld-environment-variables</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
</dict>
</plist>
